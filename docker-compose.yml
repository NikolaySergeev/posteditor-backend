version: '3.5'

services:

  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: posteditor
      MONGO_INITDB_ROOT_PASSWORD: posteditor
